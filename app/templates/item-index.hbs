<h1>{{ extension.name }}: {{ blueprint.name }}</h1>

<table class="table table-hover">
  <tr>
      <th>ID</th>
      <th>Created At</th>
  </tr>
  {{#each items}}
  <tr>
    <td>{{id}}</td>
    <td>{{created_at}}</td>
  </tr>
  {{/each}}
</table>
