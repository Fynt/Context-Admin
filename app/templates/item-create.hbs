<h1>{{ extension.name }}: {{ blueprint.name }}</h1>

{{#em-form model=this.item action="create" submit_button=false}}
  {{#each form}}
    {{blueprint-input type property=field label=label}}
  {{/each}}

  <button type="submit" class="btn btn-primary">Create</button>
{{/em-form}}
